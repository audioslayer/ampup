<UserControl x:Class="AmpUp.Views.OsdView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:controls="clr-namespace:AmpUp.Controls">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                  Background="Transparent">
        <StackPanel Margin="24,16,24,24">

            <!-- ── OSD OVERLAY ────────────────────────────────────── -->
            <Border Style="{StaticResource CardPanel}" Margin="0,0,0,12">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <Border Width="3" CornerRadius="2" Background="{DynamicResource AccentBrush}" Margin="0,0,10,0" />
                        <TextBlock Text="OSD OVERLAY" Style="{StaticResource HeaderText}" Margin="0" />
                    </StackPanel>
                    <TextBlock Text="Show overlay notifications when the app is minimized to tray."
                               Style="{StaticResource SecondaryText}" Margin="0,0,0,12" />

                    <!-- Toggles + Duration -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!-- Volume -->
                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,12,0">
                            <CheckBox x:Name="ChkOsdVolume"
                                      Content="Volume Changes"
                                      Foreground="{StaticResource TextPrimaryBrush}"
                                      ToolTip="Show overlay when you adjust a knob" />
                            <controls:StyledSlider x:Name="SldOsdVolumeDur"
                                    Minimum="1" Maximum="8" Value="2" Suffix="s"
                                    Width="200" Margin="8,0,0,0"
                                    ToolTip="How long the volume overlay stays visible" />
                        </StackPanel>

                        <!-- Profile -->
                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,12,0">
                            <CheckBox x:Name="ChkOsdProfile"
                                      Content="Profile Changes"
                                      Foreground="{StaticResource TextPrimaryBrush}"
                                      ToolTip="Show overlay when switching profiles" />
                            <controls:StyledSlider x:Name="SldOsdProfileDur"
                                    Minimum="1" Maximum="8" Value="3.5" Suffix="s"
                                    Width="200" Margin="8,0,0,0"
                                    ToolTip="How long the profile overlay stays visible" />
                        </StackPanel>

                        <!-- Device -->
                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                            <CheckBox x:Name="ChkOsdDevice"
                                      Content="Device Changes"
                                      Foreground="{StaticResource TextPrimaryBrush}"
                                      ToolTip="Show overlay when cycling audio devices" />
                            <controls:StyledSlider x:Name="SldOsdDeviceDur"
                                    Minimum="1" Maximum="8" Value="2.5" Suffix="s"
                                    Width="200" Margin="8,0,0,0"
                                    ToolTip="How long the device overlay stays visible" />
                        </StackPanel>
                    </Grid>

                    <!-- Position picker -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <StackPanel Margin="0,0,16,0">
                            <TextBlock Text="Position" Style="{StaticResource SecondaryText}" Margin="0,0,0,6" />

                            <!-- Fraps-style screen position picker -->
                            <Border Background="{StaticResource InputBgBrush}"
                                    BorderBrush="{StaticResource InputBorderBrush}"
                                    BorderThickness="1" CornerRadius="6"
                                    Width="160" Height="100" Padding="6">
                                <Grid x:Name="OsdPositionGrid">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <Border x:Name="PosTopLeft" Grid.Row="0" Grid.Column="0"
                                            Style="{StaticResource CardPanel}" Margin="2" Padding="0"
                                            CornerRadius="4" Cursor="Hand"
                                            MouseLeftButtonDown="OsdPosition_Click" Tag="TopLeft">
                                        <TextBlock Text="TL" FontSize="9" Foreground="{StaticResource TextDimBrush}"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                    <Border x:Name="PosTopCenter" Grid.Row="0" Grid.Column="1"
                                            Style="{StaticResource CardPanel}" Margin="2" Padding="0"
                                            CornerRadius="4" Cursor="Hand"
                                            MouseLeftButtonDown="OsdPosition_Click" Tag="TopCenter">
                                        <TextBlock Text="TC" FontSize="9" Foreground="{StaticResource TextDimBrush}"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                    <Border x:Name="PosTopRight" Grid.Row="0" Grid.Column="2"
                                            Style="{StaticResource CardPanel}" Margin="2" Padding="0"
                                            CornerRadius="4" Cursor="Hand"
                                            MouseLeftButtonDown="OsdPosition_Click" Tag="TopRight">
                                        <TextBlock Text="TR" FontSize="9" Foreground="{StaticResource TextDimBrush}"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                    <Border x:Name="PosBottomLeft" Grid.Row="1" Grid.Column="0"
                                            Style="{StaticResource CardPanel}" Margin="2" Padding="0"
                                            CornerRadius="4" Cursor="Hand"
                                            MouseLeftButtonDown="OsdPosition_Click" Tag="BottomLeft">
                                        <TextBlock Text="BL" FontSize="9" Foreground="{StaticResource TextDimBrush}"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                    <Border x:Name="PosBottomCenter" Grid.Row="1" Grid.Column="1"
                                            Style="{StaticResource CardPanel}" Margin="2" Padding="0"
                                            CornerRadius="4" Cursor="Hand"
                                            MouseLeftButtonDown="OsdPosition_Click" Tag="BottomCenter">
                                        <TextBlock Text="BC" FontSize="9" Foreground="{StaticResource TextDimBrush}"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                    <Border x:Name="PosBottomRight" Grid.Row="1" Grid.Column="2"
                                            Style="{StaticResource CardPanel}" Margin="2" Padding="0"
                                            CornerRadius="4" Cursor="Hand"
                                            MouseLeftButtonDown="OsdPosition_Click" Tag="BottomRight">
                                        <TextBlock Text="BR" FontSize="9" Foreground="{StaticResource TextDimBrush}"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                </Grid>
                            </Border>
                        </StackPanel>

                        <!-- Monitor selector + Preview -->
                        <StackPanel Margin="16,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="Monitor" Style="{StaticResource SecondaryText}" Margin="0,0,0,6" />
                            <StackPanel Orientation="Horizontal">
                                <ComboBox x:Name="CmbOsdMonitor" Width="280"
                                          SelectionChanged="CmbOsdMonitor_SelectionChanged"
                                          ToolTip="Which monitor to show the OSD on" />
                                <ui:Button x:Name="BtnOsdPreview" Content="Preview"
                                           Appearance="Secondary" Margin="12,0,0,0" />
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- ── QUICK WHEEL ────────────────────────────────────── -->
            <Border Style="{StaticResource CardPanel}" Margin="0,0,0,12">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <Border Width="3" CornerRadius="2" Background="{DynamicResource AccentBrush}" Margin="0,0,10,0" />
                        <TextBlock Text="QUICK WHEEL" Style="{StaticResource HeaderText}" Margin="0" />
                    </StackPanel>
                    <TextBlock Text="Hold a button to open a radial profile-switcher overlay. Turn a knob to navigate segments."
                               Style="{StaticResource SecondaryText}" Margin="0,0,0,12" TextWrapping="Wrap" />

                    <CheckBox x:Name="ChkWheelEnabled"
                              Content="Enable Quick Wheel"
                              Foreground="{StaticResource TextPrimaryBrush}"
                              Margin="0,0,0,12"
                              ToolTip="Hold the trigger button to open the radial profile switcher" />

                    <StackPanel x:Name="WheelOptions" Visibility="Collapsed">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="32" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="32" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <!-- Trigger Button -->
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Trigger Button" Style="{StaticResource SecondaryText}" Margin="0,0,0,6"
                                           ToolTip="Which physical button opens the wheel" />
                                <ComboBox x:Name="CmbWheelButton" Width="140"
                                          Background="{StaticResource InputBgBrush}"
                                          BorderBrush="{StaticResource InputBorderBrush}"
                                          Foreground="{StaticResource TextPrimaryBrush}"
                                          ToolTip="Button 1–5 to hold for the wheel">
                                    <ComboBoxItem Content="Button 1" />
                                    <ComboBoxItem Content="Button 2" />
                                    <ComboBoxItem Content="Button 3" />
                                    <ComboBoxItem Content="Button 4" />
                                    <ComboBoxItem Content="Button 5" />
                                </ComboBox>
                            </StackPanel>

                            <!-- Trigger Gesture -->
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Gesture" Style="{StaticResource SecondaryText}" Margin="0,0,0,6"
                                           ToolTip="How to trigger the wheel" />
                                <ComboBox x:Name="CmbWheelGesture" Width="120"
                                          Background="{StaticResource InputBgBrush}"
                                          BorderBrush="{StaticResource InputBorderBrush}"
                                          Foreground="{StaticResource TextPrimaryBrush}"
                                          ToolTip="Hold (500ms+) is the only supported gesture">
                                    <ComboBoxItem Content="Hold" Tag="hold" IsSelected="True" />
                                </ComboBox>
                            </StackPanel>

                            <!-- Navigation Knob -->
                            <StackPanel Grid.Column="4">
                                <TextBlock Text="Navigation Knob" Style="{StaticResource SecondaryText}" Margin="0,0,0,6"
                                           ToolTip="Turn this knob to move between wheel segments" />
                                <ComboBox x:Name="CmbWheelKnob" Width="140"
                                          Background="{StaticResource InputBgBrush}"
                                          BorderBrush="{StaticResource InputBorderBrush}"
                                          Foreground="{StaticResource TextPrimaryBrush}"
                                          ToolTip="Knob 1–5 to turn for navigation">
                                    <ComboBoxItem Content="Knob 1" />
                                    <ComboBoxItem Content="Knob 2" />
                                    <ComboBoxItem Content="Knob 3" />
                                    <ComboBoxItem Content="Knob 4" />
                                    <ComboBoxItem Content="Knob 5" />
                                </ComboBox>
                            </StackPanel>
                        </Grid>

                        <TextBlock Text="Hold the trigger button. Turn the navigation knob to spin through profiles. Release to confirm."
                                   Style="{StaticResource SecondaryText}" Margin="0,10,0,0" TextWrapping="Wrap" FontSize="11" />
                    </StackPanel>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
