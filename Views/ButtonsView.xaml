<UserControl x:Class="AmpUp.Views.ButtonsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:controls="clr-namespace:AmpUp.Controls">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border x:Name="ButtonsSurfaceBar" Grid.Row="0" Style="{StaticResource CardPanel}" Margin="0,0,0,10" Visibility="Collapsed">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                    <Border Width="3" CornerRadius="2" Background="{DynamicResource AccentBrush}" Margin="0,0,10,0" />
                    <TextBlock Text="BUTTON SURFACE" Style="{StaticResource HeaderText}" Margin="0" />
                </StackPanel>
                <controls:SegmentedControl x:Name="DeviceSelector"
                                           HorizontalAlignment="Left"
                                           MinWidth="360" />
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <Grid x:Name="TurnUpButtonsPanel" Grid.IsSharedSizeScope="True">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource CardPanel}" Margin="0,0,4,0" VerticalAlignment="Top">
                        <Grid x:Name="Btn0Grid" />
                    </Border>
                    <Border Grid.Column="1" Style="{StaticResource CardPanel}" Margin="4,0" VerticalAlignment="Top">
                        <Grid x:Name="Btn1Grid" />
                    </Border>
                    <Border Grid.Column="2" Style="{StaticResource CardPanel}" Margin="4,0" VerticalAlignment="Top">
                        <Grid x:Name="Btn2Grid" />
                    </Border>
                    <Border Grid.Column="3" Style="{StaticResource CardPanel}" Margin="4,0" VerticalAlignment="Top">
                        <Grid x:Name="Btn3Grid" />
                    </Border>
                    <Border Grid.Column="4" Style="{StaticResource CardPanel}" Margin="4,0,0,0" VerticalAlignment="Top">
                        <Grid x:Name="Btn4Grid" />
                    </Border>
                </Grid>

                <Border x:Name="StreamControllerPanel" Style="{StaticResource CardPanel}" Margin="0,10,0,0" Visibility="Collapsed">
                    <Grid x:Name="StreamControllerRoot" />
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
