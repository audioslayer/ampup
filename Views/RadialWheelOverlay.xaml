<Window x:Class="AmpUp.Views.RadialWheelOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="460" Height="460"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Storyboard x:Key="FadeIn">
            <DoubleAnimation Storyboard.TargetName="RootGrid"
                             Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.2">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut" />
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
        <Storyboard x:Key="FadeOut">
            <DoubleAnimation Storyboard.TargetName="RootGrid"
                             Storyboard.TargetProperty="Opacity"
                             From="1" To="0" Duration="0:0:0.2">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn" />
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Window.Resources>

    <!-- Root canvas: transparent background to capture mouse -->
    <Grid x:Name="RootGrid" Opacity="0" KeyDown="Window_KeyDown" Focusable="True">

        <!-- Outer dark ring / glass circle -->
        <Ellipse Width="420" Height="420"
                 HorizontalAlignment="Center" VerticalAlignment="Center">
            <Ellipse.Fill>
                <SolidColorBrush Color="#CC141414" />
            </Ellipse.Fill>
            <Ellipse.Effect>
                <DropShadowEffect x:Name="OuterGlow" BlurRadius="30" Opacity="0.25" ShadowDepth="0" />
            </Ellipse.Effect>
        </Ellipse>

        <!-- Segment canvas — pie paths drawn in code-behind -->
        <Canvas x:Name="SegmentCanvas"
                Width="420" Height="420"
                HorizontalAlignment="Center" VerticalAlignment="Center" />

        <!-- Center circle overlay -->
        <Ellipse Width="120" Height="120"
                 HorizontalAlignment="Center" VerticalAlignment="Center">
            <Ellipse.Fill>
                <SolidColorBrush Color="#EE0F0F0F" />
            </Ellipse.Fill>
            <Ellipse.Effect>
                <DropShadowEffect Color="Black" BlurRadius="12" Opacity="0.5" ShadowDepth="0" />
            </Ellipse.Effect>
        </Ellipse>

        <!-- Center profile name label -->
        <TextBlock x:Name="CenterLabel"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   TextAlignment="Center"
                   FontSize="13" FontWeight="SemiBold"
                   Foreground="#E8E8E8"
                   MaxWidth="110" TextWrapping="Wrap" />

    </Grid>
</Window>
